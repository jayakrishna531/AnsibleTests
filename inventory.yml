# inventory.yml with password placeholder
webservers:
  hosts:
      server1:
        ansible_connection: ssh
        ansible_host: "IP"
        ansible_user: username
        ansible_password: "<password_here>"